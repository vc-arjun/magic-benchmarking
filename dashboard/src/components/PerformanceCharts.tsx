import React from 'react';
import { BenchmarkResults } from '@/types/reports';

type Props = {
  data: BenchmarkResults;
};

export const PerformanceCharts: React.FC<Props> = ({ data }) => {
  return <div>PerformanceCharts</div>;
};
