name: Benchmark and Deploy

on:
  workflow_dispatch:
    inputs:
      config:
        description: 'Custom configuration JSON (optional - uses default if not provided)'
        required: false
        type: string

jobs:
  benchmark-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd dashboard && npm ci

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright
        run: npx playwright install --with-deps chromium

      - name: Download existing reports
        run: |
          echo "📥 Downloading existing reports from deployed site..."
          mkdir -p dashboard/public
          # Try to download existing reports.json from GitHub Pages
          if curl -f -s -o dashboard/public/reports.json "https://${{ github.repository_owner }}.github.io/magic-benchmarking/reports.json"; then
            echo "✅ Downloaded existing reports.json"
            echo "📊 Existing reports count: $(cat dashboard/public/reports.json | jq length 2>/dev/null || echo 'unknown')"
          else
            echo "ℹ️  No existing reports.json found (first run or site not deployed yet)"
            echo "[]" > dashboard/public/reports.json
          fi

      - name: Run benchmarking
        run: npm start
        env:
          MAGIC_BENCHMARKING_CONFIG: ${{ github.event.inputs.config }}

      - name: Build dashboard
        run: npm run web:build

      - name: Verify final reports
        run: |
          echo "📊 Final reports summary:"
          if [ -f "dashboard/public/reports.json" ]; then
            total_reports=$(cat dashboard/public/reports.json | jq length 2>/dev/null || echo 0)
            performance_reports=$(cat dashboard/public/reports.json | jq '[.[] | select(.type == "performance")] | length' 2>/dev/null || echo 0)
            network_reports=$(cat dashboard/public/reports.json | jq '[.[] | select(.type == "network")] | length' 2>/dev/null || echo 0)
            echo "  📈 Total reports: $total_reports"
            echo "  ⚡ Performance reports: $performance_reports"
            echo "  🌐 Network reports: $network_reports"
          else
            echo "  ❌ No reports.json found"
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard/out

      - name: Deploy to Pages
        uses: actions/deploy-pages@v4